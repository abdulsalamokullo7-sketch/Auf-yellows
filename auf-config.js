/**
 * Optional: cloud backup API base URL (no trailing slash), when you host an endpoint.
 * Leave empty — file backup + on-device storage still work.
 */
window.AUF_CONFIG = window.AUF_CONFIG || {};
window.AUF_CONFIG.cloudBackupBaseUrl = "";
